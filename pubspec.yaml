name: simple_notes
description: A simple notes app with Hive storage.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=3.0.0"
  
dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.1     # Access device storage
  flutter_quill: ^8.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

assets:
  - assets/images/